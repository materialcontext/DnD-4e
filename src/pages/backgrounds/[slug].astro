---
import ClassLayout from "../../layouts/bgLayout.astro";

// change this to the proper api endpoint
let response = await fetch(`${Astro.site}data/backgrounds/${Astro.params.slug}`);
let data = await response.json();

---
<ClassLayout data={data}/>