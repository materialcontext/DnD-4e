---
import RaceLayout from "../../layouts/raceLayout.astro";
import "../../styles/style.css"

// change this to the proper api endpoint
let response = await fetch(`D:/FPXI/Programming/DnD-4e/src/data/races/${Astro.params.race}.json`);
let data = await response.json();


---
<RaceLayout data={data}/>