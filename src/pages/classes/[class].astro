---
import ClassLayout from "../../layouts/classLayout.astro";
import "../../styles/race.css"

// change this to the proper api endpoint
let response = await fetch(`D:/nodejs_programs/DnD-4e/src/data/classes/${Astro.params.class}.json`);
let data = await response.json();

---
<ClassLayout data={data}/>